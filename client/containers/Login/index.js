import React from 'react';
import {Input} from '../../components/form';

const Login = (props) => (
  <div>
    <p>Please login using the dropdown in the sidebar.</p>
  </div>
);

export default Login;
